# V0.20 implementation report

This pass adds bounded plan validity and explicit reconciliation APIs. It does not
add a planner, production scheduler, or automatic agent execution. This report
separates implemented mechanisms from evidence that has not been established.

| # | Requested item | Result |
|---|---|---|
| 1 | Files | `src/plan`, `src/store/plan.rs`, `src/cli/plan.rs`, `tests/plan.rs`, deployment fixture, runtime/Bridge integrations and documentation. |
| 2 | Migration | 025: definitions, immutable revisions, runs, immutable observations/assessments/step records/snapshots, events. Old JSON is preserved. |
| 3 | ExecutionPlan | Explicit goal, positive revision, 1–100 step DAG, assumptions, invariants, checkpoints, commitments, knowledge and component pins. |
| 4 | PlanStep | Skill, Composition, Tool, Effect, Observe, Experiment, Approval, Custom. Custom execution fails closed. |
| 5 | Revision semantics | Monotonic immutable revisions with stored content hashes; edits to completed work cannot be carried forward as a replan. |
| 6 | Assumptions | Typed predicates, source, dependent steps, evidence and severity. |
| 7 | Sources | Assessment derives support from evidence; declared validity is not trusted. |
| 8 | Freshness | Expiry, observation age, external versions, authoritative refresh and mutation epochs. Critical and commitment limits are stricter. |
| 9 | Invariants | Entire plan, ordered interval, checkpoint boundary, before/after commitment scopes. |
| 10 | Checkpoints | Observation and dependency checks; immutable snapshots. |
| 11 | Commitment points | Explicit effects, invalidated assumptions, lost and newly required recoveries. |
| 12 | Gates | Assumptions, invariants, recoveries, external approval, diversity. No authority grant. |
| 13 | State | Completed/current step, observations, epochs, checkpoints, commitments, external versions and authorization references. |
| 14 | Assessment | Valid/warnings, verify, replan, recovery, invalid/unknown with blockers and recommendations. |
| 15 | Algorithm | Deterministic next-step dependency evaluation; no model calls. |
| 16 | Drift | Trusted value/source changes and stale evidence are recorded separately from causal findings. |
| 17 | Impact graph | Assumption-to-step dependencies and dependent-step closure. |
| 18 | Incremental checks | Relevant next-step assumptions plus active invariants/checkpoints/gates; bounded structural validation still traverses the plan. |
| 19 | Observations | Duplicate keys, future reports and replacement of newer authoritative state are rejected. Arbitrary attestation-backed claims require a verified output adapter. |
| 20 | Knowledge | Revision/activation/freshness checks and normal hierarchy resolution; publication rechecks current state. |
| 21 | Nested composition | Pins, assumptions, entire-sequence invariants, stricter commitment freshness, engine-result reconciliation and receipt-derived nested recovery state. |
| 22 | Runtime | Plan validity follows hard authority/policy checks. Bridge discards supplied assessments. Publication detects intervening changes. |
| 23 | Replan | Explicit saved revision; preserves completed steps and commitments, clears approvals, retains prior run history. |
| 24 | Resume | Reassesses the live run; never restores old effect state from a snapshot. |
| 25 | Recovery | Reads actual receipts, including partial completion. Failure requires recovery or explicit replanning. No fictitious rollback. |
| 26 | Causal integration | Plan drift can create a V0.14 investigation. Temporal adjacency alone does not establish a cause. |
| 27 | Prediction | Plan trajectories record drift and mutation features; existing forecast engine is queried. No new validated predictive signature is claimed. |
| 28 | Curriculum | Five plan goal kinds; concrete local shell experiments run through the existing executor and budgets. |
| 29 | Economics | Plan validation opportunities use existing exposure, severity, cost and learning-value scoring. |
| 30 | Assurance | Optional `plan-continuity-basic-v1`; family-specific continuity remains inconclusive without a registered evaluator. No arbitrary goal certification. |
| 31 | GuardCandidate | Normal hierarchy/Guard review remains authoritative. This pass does not automatically derive a GuardCandidate from plan evidence. |
| 32 | CLI | List/import/show/inspect/assumptions/checkpoints/start/status/why/history/replay/diff/checkpoint/resume/replan/recovery/forecast/validate. |
| 33 | Dependency drift | Deterministic evaluator requests replan; isolated deployment comparison records fail/fail/pass across three arms. |
| 34 | Stale observation | Stale truth requires verification rather than being labelled contradicted. |
| 35 | Unknown critical fact | Missing and agent-reported facts do not satisfy critical assumptions. |
| 36 | Irrelevant drift | Unrelated assumptions do not interrupt continuation. |
| 37 | Commitment gate | Freshness, missing approval/recovery and common-mode blockers are tested. |
| 38 | Expired approval | Runtime checks stored effect-bound authorizations and expiry; no dedicated end-to-end expired-approval plan fixture in this pass. |
| 39 | Knowledge change | Current revision, activation and freshness are checked; revision invalidation is tested. |
| 40 | Checkpoint resume | Fresh resume remains valid; changed current facts require replanning while the old snapshot remains intact. |
| 41 | Nested commitment | Receipt state and freshness propagation implemented; no dedicated nested commitment end-to-end fixture yet. |
| 42 | Partial recovery | Receipt-derived reconciliation and failure/replan lifecycle tested; a mock effect commits before an unfinished step is replaced, and its receipt survives replanning. No production effects. |
| 43 | Predictive drift | Integration exists; predictive calibration/early-warning validation remains future evidence work. |
| 44 | Causal drift | Controlled continuation comparison exists; no plan-specific promoted causal mechanism is claimed. |
| 45 | Common mode | Gates consume the existing epistemic diversity report; low-diversity gate behavior is tested. |
| 46 | Unnecessary replan | Replans remain inconclusive until controlled evidence establishes appropriateness. No inferred success label. |
| 47 | Performance | Stable 10/25/50/100-step evaluation is tested. Timing samples are engineering measurements, not an SLA. |
| 48 | Comparison | One deterministic deployment case, three isolated candidate branches: static 0/1, checkpoint-only 0/1, adaptive 1/1. No live deployment effects. |
| 49 | Limits | No agent scheduler, arbitrary predicates are unknown, no family-specific certification or new predictive model, no generic non-shell Skill adapter. |
| 50 | Deviations | The optional certification/Guard promotion and several requested end-to-end evidence fixtures remain incomplete. The implemented mechanisms fail closed where their adapters/evidence are absent. |

## Validation

Final validation results are recorded below after the last source changes. An earlier
full run found three schema-version expectation failures (24 versus 25); those assertions
were updated. One earlier causal test transiently remained Testable; its targeted rerun
and the subsequent full causal suite passed. Its failure diagnostic now includes the
recorded experiment report rather than obscuring an inconclusive result.

- Full serial all-target/all-feature suite: 377 passed, 0 failed, 5 ignored.
- Final focused regression suite after the last fixes: 132 passed, 0 failed, 2 ignored.
- Final formatting, all-target/all-feature Clippy with warnings denied, and diff whitespace checks passed.
- Performance and controlled comparison samples: `docs/plan-benchmark.json`.
